# Base API URL pointing to the Django backend.
# Locally, this defaults to: http://127.0.0.1:8000/api
# In production (Vercel), set this to your live Render backend URL + /api (e.g., https://your-backend.onrender.com/api)
VITE_API_BASE_URL=http://127.0.0.1:8000/api
